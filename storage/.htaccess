## Deny access to all files in storage directory
## Prevents direct web access to sensitive data:
## - session files (may contain sensitive data)
## - cached settings (may contain API secrets)
## - log files (may contain stack traces)
## - uploaded media (if stored in non-public disk)

Order Deny,Allow
Deny from all
